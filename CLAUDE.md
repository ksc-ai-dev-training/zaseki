# 本社座席予約システム（Zaseki）

新人社員向けAI開発研修の題材プロジェクト。東京本社のフリーアドレス座席予約を、現行のGoogleスプレッドシート運用からWebシステムに移行する。

## 参照ドキュメント

作業を始める前に、必ず以下を確認すること。

- **要求仕様書**（発注者からの要求、v1.0）: `docs\座席予約システム_要求仕様書.html`
- **要件定義書**（要求を具体化した要件、最新版）: `docs\座席予約システム_要件定義書.html`
  - 版数・改訂履歴は文書内の「改訂履歴」表を参照。10章に暫定方針（D1〜D6）と引き続き確認が必要な事項を整理してある。
- **基本設計書**: `docs\座席予約システム_基本設計書.html`
- **詳細設計書**（作成中）: `docs\座席予約システム_詳細設計書.html`

## ドキュメント作成の進め方

- 要求仕様書 → 要件定義書 → 基本設計書 → 詳細設計書 の順に、後続ドキュメントで実現方式・画面構成・データ構造を具体化していく。
- 各ドキュメントはHTML（要求仕様書と同じCSSスタイル）で作成し、`docs\` 配下に置く。
- 発注者からの訂正・追加情報は都度ドキュメントに反映し、改訂履歴に版数を上げて記録する。
- ドキュメントは一気に全部作らず、段階的に（章単位・トピック単位で）進める。

## ID体系・相互参照（Keireki踏襲）

後から付けるのは大変なため、要素を新設する時点で必ずIDを振り、関連ドキュメント間で相互参照する。

- `REQ-F-xx` / `REQ-N-xx`: 要求仕様書の機能要求・非機能要求
- `FR-xx-y`: 要件定義書の機能要件（`REQ-F-xx 対応` の節に属する。要求仕様書に明記のない追加提案は「要求仕様書には明記のない追加提案」と明記し、REQ-F-xxとの対応をつけない）
- `S-xx`: 画面（基本設計書2.2節・画面モックアップ）
- `T-xx`: テーブル（基本設計書3.2節・詳細設計書2章）
- `A-xx`: APIエンドポイント（詳細設計書3章。新しいエンドポイントを追加したら必ずAxxを振る）
- 各ドキュメントの記述中で、対応するIDを括弧書きで添える（例:「（FR-03-9、S-09）」「（A-12、S-04が使用）」）。「この画面は何の要求から来たのか」を後から辿れることを目的とする。
- 詳細設計書1章に、REQ→FR→S-xxの対応をまとめたトレーサビリティ表を置く（新しい要求・画面を追加したら更新する）。

## 検討資料の扱い（Keireki踏襲）

- AIに相談して出てきた検討メモ・比較検討・暫定計画は、正式な成果物（`docs\`）に混ぜず、`検討資料\`（プロジェクトルート直下、`docs\`と並列）に分けて置く。
- ファイル名は `YYYYMMDD_タイトル.html`（または`.md`）とし、冒頭にステータス（検討中／採用／見送り）を明記する。
- 検討資料を追加・更新したら `検討資料\README.md` の一覧に1行追加・更新する。
- 承認されて設計に反映されたら、内容は該当設計書へ取り込み、検討資料側は「採用／設計反映済み（バージョンを明記）」のようにステータスを更新して経緯の記録として残す（削除しない）。
- 決まったことだけを設計書に書き、迷った経緯・比較した案は検討資料に残す、という使い分けを徹底する。
.env は絶対にpushしないこと